// class Multiple {
//   constructor(x:number, y:number);
//   constructor(x:number);
//   public constructor(...args_arr:any[]){
//     if(args_arr.length === 1){
//       console.log("There is one argument in constructor");
//     }
//     if (args_arr.length === 2) {
//       console.log("There is two argument in constructor");
      
//     }
//   }
// }

// let multiObj = new Multiple(2);
// let multiObj2 = new Multiple(2,5);